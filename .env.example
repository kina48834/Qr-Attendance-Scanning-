# Copy to `.env` for local dev. On Vercel, set `VITE_*` in Project → Settings → Environment Variables.
# Replace the placeholder keys with values from Supabase → Settings → API.

VITE_SUPABASE_URL=https://bvisyesucoamqzckiesz.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_REPLACE_WITH_YOUR_ANON_KEY

# Server-side only (do not expose in browser bundles)
SUPABASE_SECRET_KEY=sb_secret_REPLACE_WITH_YOUR_SECRET_KEY
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.bvisyesucoamqzckiesz.supabase.co:5432/postgres
